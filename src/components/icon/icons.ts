import { FolderInput, FolderOutput, FolderSync, Search } from 'lucide-react';
import { Spinner } from './spinner';

export const icons = {
  magnify: Search,
  spinner: Spinner,
  'tx-in': FolderInput,
  'tx-out': FolderOutput,
  'tx-self': FolderSync,
};

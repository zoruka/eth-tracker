import {
  ArrowRight,
  FileBadge,
  FolderInput,
  FolderOutput,
  FolderSync,
  Search,
  Wallet,
} from 'lucide-react';
import { Spinner } from './spinner';

export const icons = {
  'arrow-right': ArrowRight,
  contract: FileBadge,
  magnify: Search,
  spinner: Spinner,
  'tx-in': FolderInput,
  'tx-out': FolderOutput,
  'tx-self': FolderSync,
  wallet: Wallet,
};
